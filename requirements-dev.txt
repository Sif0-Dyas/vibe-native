# Development / test dependencies. Install:  pip install -r requirements-dev.txt
-r requirements.txt
pytest-cov>=5
ruff>=0.6
bandit>=1.7
pre-commit>=3.5   # git hooks that run ruff on commit (see .pre-commit-config.yaml)
# packaging (manual `python tools/build_exe.py`; not used by the test suite or CI)
pyinstaller>=6
pywebview>=5.0
